/**
 * Mapa vazio: telas de auth usam tipografia do sistema (`authFontFamilies` em `authTheme.ts`).
 * Mantido para compatibilidade com `useFonts` em `app/_layout.tsx`.
 */
export const authFontsMap = {} as Record<string, unknown>;
